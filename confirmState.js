function confirmState(departure, destination, price) {
    background(255);
    text(departure, windowWidth/2.2, windowHeight/2);
    text(destination, windowWidth/1.8, windowHeight/2);
    text(price, windowWidth/2, windowHeight/2.2);
}